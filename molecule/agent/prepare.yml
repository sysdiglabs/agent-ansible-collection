---
- name: Prepare
  hosts: all
  gather_facts: true
  become: true
  tasks:
    - name: Make sure python3 is installed
      package:
        name: python3
        state: present

    - name: Update apt cache
      ansible.builtin.apt:
        update_cache: true
      when: ansible_distribution == "Debian"
